# Summarizer Plugin

Condenses long text into concise bullet summaries using deterministic
extractive ranking (no LLM required to demonstrate the plugin contract).

Install: `kage install summarizer` (auto-discovered from `kage/plugins/`).
